{
  "extends": "stylelint-config-standard",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": [
          "theme",
          "plugin",
          "apply",
          "layer",
          "config",
          "tailwind"
        ]
      }
    ]
  }
}
