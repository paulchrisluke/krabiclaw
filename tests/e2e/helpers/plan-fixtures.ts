/** The Growth tenant e2e drives MCP with: Ember & Slice, the demo organization's owner. */
export const MCP_GROWTH_USER_ID = 'user-e2e-demo-owner'
/** Owns no organization; ensureSite() creates one through Better Auth so the run starts from nothing. */
export const MCP_GROWTH_SERVICE_USER_ID = 'user-e2e-growth-service-owner'
